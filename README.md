# 活動靈感大對決

給傳說對決營運團隊使用的內部工具，主打「急需活動點子時」快速使用，四大功能：

1. **我要找活動靈感**（對應檔案 `generate.html`）：選一個這次想達成的目標（提升主要模式時長／其他模式時長／非對局在線時長／登入率），立即看到靈感庫中標註對應目標的活動點子。（首頁主打功能）
2. **我有H5活動靈感**（對應檔案 `ideas.html`）：同事自由上傳的 H5 小遊戲點子，每則點子都標註提供者，可附上示意圖、Demo 網頁連結（選填）、附件，供大家瀏覽、互相激發靈感。
3. **我有熱點活動靈感**（對應檔案 `hotideas.html`）：跟時事熱點、話題結合的活動靈感，上傳/編輯/刪除邏輯跟「我有H5活動靈感」完全一樣，只是資料來源分開。
4. **過往歷史活動**（對應檔案 `activities.html`）：彙整過往刺激活躍的 H5 小遊戲活動——機制、設計目的、特殊需求、成效數據，可依目的／特殊需求／遊戲類型篩選。設計新活動前，先看看過去做過什麼、效果如何。

## 兩個版本：示範版 vs 正式共用版

這個專案其實有**兩份前端**，服務不同目的：

| | 示範／預覽版 | 正式共用版 |
|---|---|---|
| 網址 | `changti-dotcom.github.io/h5-activity-hub/`（GitHub Pages） | `script.google.com/.../exec`（部署後才有，見下方） |
| 檔案 | 根目錄的 `index.html` / `activities.html` / `ideas.html` / `hotideas.html` / `generate.html` / `js/*.js` | `apps-script/webapp/` 底下的樣板，執行在 Google Apps Script 上 |
| 資料 | 種子範例資料，存在瀏覽器本機 localStorage，別人看不到你新增或編輯的內容 | 真的讀寫團隊共用的 Google Sheet，同事新增/編輯/刪除都是即時共用 |
| 存取限制 | 任何人都能開（公開網頁） | 僅限已登入 Garena Google 帳號的同事（公司資安政策要求） |
| 用途 | 快速看介面長怎樣、示範用 | 團隊實際使用 |

會有兩份的原因：公司政策不允許 Apps Script 部署開放「所有人」存取，只能「僅限公司網域」，而網域限定的部署沒辦法被外部網站（GitHub Pages）用背景 `fetch()` 呼叫（會被導向 Google 登入頁），所以正式版乾脆整個網站都跑在 Apps Script 上，同源讀寫沒有這個問題。詳見 [`apps-script/SETUP.md`](apps-script/SETUP.md)。

**重要**：示範版是純展示用途，任何人在網站上新增/編輯的靈感只存在自己瀏覽器裡，其他人（包含重新整理的自己）都看不到。要達到「同事新增或更新靈感後，其他收到連結的同事都看得到」，必須部署正式共用版（見下方）。

## 目前狀態

- 架構與介面已完成，含紅白 Garena 主題與傳說對決 logo。
- **成效數據尚未整理**：過往歷史活動裡的「成效數據」欄位目前多數是空的（會顯示「待補充」），待營運數據整理好後再補上即可，不影響現在使用其他功能。
- **設計目的**多數活動也還沒分類（少數機制本身已明確指出目的的，如「回歸之路」「建造大作戰」已先標註），需要團隊之後陸續補齊。
- 正式共用版尚未部署完成——需要照 [`apps-script/SETUP.md`](apps-script/SETUP.md) 的步驟建立 Google Sheet、貼上 Apps Script 程式碼與樣板、部署，才會有同事能用的正式網址。

## 怎麼預覽示範版

示範版是純靜態網頁，不需要安裝任何東西：

- 直接用瀏覽器打開 `index.html` 即可（雙擊檔案，或右鍵→開啟檔案）。
- 或直接看已經上線的 GitHub Pages 版本：`changti-dotcom.github.io/h5-activity-hub/`（記得強制重新整理 Ctrl+F5，避免看到瀏覽器快取的舊版畫面）。

## 部署正式共用版

詳細步驟（含建立 Google Sheet、貼上 Apps Script 程式碼與網頁樣板、部署）都在 [`apps-script/SETUP.md`](apps-script/SETUP.md)，10-15 分鐘可以做完，不需要工程師、不需要架伺服器。

## 檔案結構

```
index.html             首頁：總覽 + 四大功能入口
generate.html          我要找活動靈感頁面（依目標篩選靈感庫）
js/generate.js         我要找活動靈感頁面邏輯
activities.html        過往歷史活動頁面（篩選 / 列表 / 詳情 / 新增活動表單）
js/activities.js       過往歷史活動頁面邏輯
ideas.html             我有H5活動靈感頁面（篩選 / 列表 / 詳情 / 新增點子表單，含編輯/刪除）
hotideas.html          我有熱點活動靈感頁面（與 ideas.html 共用同一套邏輯）
js/ideas.js            我有H5/熱點活動靈感共用頁面邏輯
css/style.css          全站樣式（Garena 紅白主題）
js/config.js           共用資料庫網址設定（正式部署後填入）
js/api.js              資料讀寫邏輯（示範模式 / 正式模式自動切換）
js/common.js           共用標籤定義（設計目的／特殊需求／遊戲類型／目標）與 UI 輔助函式
js/mock-data.js        種子資料：過往 H5 小遊戲活動 + 範例點子
assets/aov-logo.png    傳說對決去背 logo
apps-script/Code.gs     正式共用版後端：共用資料庫讀寫 + 網頁樣板路由（部署到 Google Apps Script）
apps-script/SETUP.md    正式共用版部署步驟教學
apps-script/webapp/     正式共用版的網頁樣板（Shared/Index/Activities/Ideas/HotIdeas/Generate），需複製貼上到 Apps Script 編輯器
```

## 「我要找活動靈感」怎麼運作

選一個目標（提升主要模式時長／其他模式時長／非對局在線時長／登入率），立即篩選出「我有H5活動靈感」靈感庫裡標註對應目標的點子。這是純篩選功能，不呼叫 AI；同事新增點子時在「進階資訊 → 設計目的」勾選對應目標即可被篩選到。

## 標籤分類（可在 `js/common.js` 調整）

- **我要找活動靈感 / 靈感庫「設計目的」**：提升主要模式時長 / 提升其他模式時長 / 提升非對局在線時長 / 提升登入率
- **過往歷史活動「設計目的」**：拉新 / 回流 / 留存 / 付費 / 版本導流 / 品牌口碑
- **特殊需求**：時事熱點 / 節慶檔期 / IP聯動 / 週年慶 / 新版本上市 / 電競賽事聯動 / 老玩家回歸 / 低開發成本 / 需搭配抽卡機制
- **遊戲類型**：益智消除 / 反應操作 / 心理測驗 / 經營模擬 / 任務養成 / 抽獎機率 / 社交邀請 / 陣營競賽 / 劇情回顧 / 音樂節奏 / 其他

想調整或新增分類，直接編輯 `js/common.js` 裡對應的常數陣列即可，相關頁面的篩選器與表單都會自動套用新的分類。

## 之後可以考慮擴充的方向（目前尚未實作）

- 「我有H5活動靈感 / 我有熱點活動靈感」的「+1 共鳴」或留言功能，幫助團隊看出哪些點子最多人有共鳴。
- 記錄「是哪個 Garena 帳號」新增的資料（目前部署雖然限制僅 Garena 帳號能存取網站，但送出表單時的姓名/暱稱仍是手動輸入，沒有跟登入帳號綁定）。
- 真正的圖片/檔案上傳功能（目前都是貼連結，例如 Google Drive 分享連結）。
